{
	"name": "@harness-template/api",
	"version": "0.0.0",
	"private": true,
	"type": "module",
	"scripts": {
		"build": "bunx tsc -p tsconfig.json",
		"dev": "bun --watch src/index.ts",
		"lint": "biome check .",
		"typecheck": "bunx tsc -p tsconfig.json --noEmit",
		"test": "bun test"
	},
	"dependencies": {
		"@harness-template/shared": "workspace:*"
	}
}
