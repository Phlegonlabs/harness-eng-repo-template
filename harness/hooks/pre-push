#!/usr/bin/env bash
set -euo pipefail

echo "harness: running pre-push structural tests..."
echo ""

if bun run harness:guardian --mode stop --no-state; then
  echo ""
  echo "harness: pre-push checks passed."
else
  echo ""
  echo "harness: pre-push checks FAILED."
  exit 1
fi
